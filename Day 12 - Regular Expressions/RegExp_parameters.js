// RegExp parameters
// Pattern n flag

let pattern = 'love'
let flag = 'gi'
let regEx = new RegExp(pattern, flag)

// or regEx = new RegExp('love','gi')

// Creating a pattern without RegExp Constructor

regEx= /love/gi